interface:
  display_name: "Verilog RTL Workflow"
  short_description: "RTL design with timing, CDC, CBB reuse, lint, and simulation."
  default_prompt: "Use $verilog-rtl-workflow to scan for reusable CBBs, derive the module contract, implement synthesizable timing-aware RTL, review CDC risks, and complete lint, TB, and simulation work."

policy:
  allow_implicit_invocation: true
