interface:
  display_name: "NCS Datasheet Gen"
  short_description: "Template-safe chip datasheet DOCX drafts"
  default_prompt: "Use $ncs-datasheet-gen to generate a planning chip datasheet DOCX from template and competitor/history inputs. Confirm all input gates first, normalize datasheet_model, run stage-1 short-field DOCX patch with template-fidelity checks, then perform controlled block replacement and visual convergence."
